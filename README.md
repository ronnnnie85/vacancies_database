# Проект по работе с вакансиями с сайта hh.ru

## 📌 Описание

Данный проект предназначен для интеграции с API сайта [hh.ru](https://hh.ru), получения информации о работодателях и их вакансиях, сохранения этих данных в базу данных PostgreSQL и предоставления различных способов анализа данных через интерфейс командной строки.

## 🛠 Стек технологий

- Python 3.10+
- PostgreSQL
- Psycopg2
- requests
- poetry

## 📁 Структура проекта

```
Vacancies_database/
├── src/
│   ├── config.py               # Загрузка конфигурации БД
│   ├── database_filling.py     # Создание БД и таблиц, заполнение данными
│   ├── db_manager.py           # Класс DBManager для запросов к БД
│   └── headhunter_api.py       # Класс HeadHunterAPI для работы с API
├── main.py                     # Точка входа, взаимодействие с пользователем
├── database.ini                # Конфигурационный файл PostgreSQL
├── .gitignore
├── pyproject.toml              # Зависимости Poetry
└── README.md                   # Документация
```

## 📚 Функциональность

### Работа с API

Класс `HeadHunterAPI`:
- `load_employer(employer_id: str)` — загружает данные о работодателе.
- `load_vacancies(employer_id: str)` — загружает вакансии работодателя.

### Работа с базой данных

Класс `DatabaseFilling`:
- Автоматическое создание базы данных и таблиц `employers`, `vacancies`.
- Методы:
  - `employers_filling(employers: list)`
  - `vacancies_filling(vacancies: list)`

### Запросы к БД

Класс `DBManager`:
- `get_companies_and_vacancies_count()` — список компаний с количеством вакансий.
- `get_all_vacancies()` — список всех вакансий с указанием компании, зарплаты, ссылки.
- `get_avg_salary()` — средняя зарплата по вакансиям.
- `get_vacancies_with_higher_salary()` — вакансии с зарплатой выше средней.
- `get_vacancies_with_keyword(keyword: str)` — вакансии по ключевому слову.

### Пользовательский интерфейс

- Взаимодействие осуществляется через консоль.
- Есть возможность выбрать нужное действие из меню.
- Учитывается валидация имени базы данных.
- Реализовано обновление данных по запросу пользователя.

## 🔒 Безопасность

- Данные подключения к БД хранятся в `database.ini` и не входят в git благодаря `.gitignore`.


## 🧪 Установка и запуск

1. Установите зависимости:
```bash
poetry install
```

2. Запустите проект:
```bash
poetry run python main.py
```

3. Следуйте инструкциям в консоли.


## ⚙️ Настройка database.ini

Перед запуском проекта необходимо создать файл `database.ini` в корне проекта со следующей структурой:

```ini
[postgresql]
host=localhost
port=5432
user=ваш_пользователь
password=ваш_пароль
```

Убедитесь, что данные подключения к PostgreSQL корректны и соответствуют вашей системе.


## 📄 Лицензия

MIT License
